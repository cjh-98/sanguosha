## 修复三国杀退出/日志/UI残留Bug（第三轮：彻底根治）

### 第一轮（已废弃）
尝试通过 _timers 追踪系统 + destroy() 方法解决，但做得不彻底。

### 第二轮
发现 3 处 game.js 核心循环中直接用 setTimeout 绕过 _timers，全部改为 this._setTimer。

### 第三轮：真正的根因
**浏览器缓存旧版 game.js**，导致 engine.destroy() 因访问不存在的新字段（_timers）而抛错，后续清理代码全部跳过，游戏继续运行。

### 修复（4处）
1. `quitToMenu()` — try-catch 包裹 engine.destroy()，加 clearTimeout(1~9999) 暴力清理
2. `restartGame()` — 同上
3. `game.js notifyState()` — 新增 if(this._destroyed) return 守卫
4. `_sgsCollectGameState()` — 改为 try-catch 包裹 engine 访问

### 核心教训
给已有项目加新 API 时，调用方必须 try-catch 兜底，防止缓存导致类型错误中断整个流程。

## Bug 自动修复任务 + 部署

- 自动化任务检查并处理了 31 个 GitHub Issues
- Issue #74: 添加了许褚武将图片（xuchu.jpg）
- 代码已推送到 GitHub (commit: 4374c13)
- **已重新部署到 CloudStudio 沙箱**
- 分享链接: https://12ccce01093947feba17f92e173d0667.tc-nanjing.share.codebuddy.woa.com

## 更新自动化任务

更新了 Bug 自动修复检查任务的 prompt，主要改进：
1. **添加部署上线步骤**：有代码/资源变更时必须调用 workbuddy_cloudstudio_deploy 部署
2. **改进标签区分**：
   - solved: 有实际修复（代码/资源修改）
   - closed: 无实际修复（日志不完整、日志正常等）
   - unsolved: 确实无法解决
3. **无修复时也要关闭 issue**：添加 closed 标签并关闭

## UI 美化：武将牌/手牌/头像尺寸放大 + 间距优化

- **武将牌 hero-card**: 76→100px 宽, 110→142px 高, 立绘 80→110px
- **自己头像 my-avatar**: 36→48px, 边框增强+光晕
- **手牌 card**: 68→74px 宽, 96→104px 高, 边框 2→2.5px
- **弧形区域**: gap:4→8px, justify-content:center 居中, min-height:110→155px
- **光效增强**: .cur 选中 + hover 光晕
- Commit: 9d31850, 已自动部署到 https://cjh-98.github.io/sanguosha/

## UI 二次修复：4大问题（黑横条/响应式/头像/手机适配）

用户反馈4问题，工程师agent不可用，主理人亲自修复：
1. **删不掉的黑横条**：真凶是废弃的空日志框 `#gameLog`（日志早迁到右侧#glog面板，空框带黑底边框显示成横条）。删元素+删CSS `.game-log`+删JS引用数组里的'gameLog'。
2. **对手区太密+左右留空**：`.players-arc-area` justify-content改space-evenly铺满，gap/padding用vw；`.hero-card`尺寸改 `clamp(72px,8.5vw,104px)` + `aspect-ratio:5/7`；立绘改flex:1自适应（原固定110px高）。
3. **头像太矮**：`.my-avatar` 48x48 → 72x98px。
4. **手机适配**：新增 `@media(max-width:900px)` + 竖屏媒体查询；`100dvh`避免地址栏遮挡；卡牌缩小保证一屏三区可见。
- Commit: d695df5

### 坑：#rotateHint ID 冲突
仓库**早已有**一个全屏"请横屏使用"遮罩 `#rotateHint`（body顶层763行，display:none禁用态，带.rh-icon/.rh-text/.rh-sub）。我误加了同名浮条CSS+HTML造成ID重复冲突。已回退删除，保留原有遮罩禁用态，竖屏走响应式布局让用户可玩。Commit: 7c22cc1。
**教训**：改已有大文件前先 grep 目标ID/class 是否已存在，避免重复定义冲突。

## 移动端显示修复（竖屏遮罩 + 横屏看不到画面）

用户反馈：
1. 竖屏时仍看到"请横屏使用"黑色遮罩（display:none不可靠）
2. 横屏时看不到游戏画面（height:100%在很多浏览器不生效）

修复：
1. **删除整个 #rotateHint**：移除元素+CSS+@keyframes（用户明确不要强制横屏，竖屏也要能玩）
2. **height:100% → 100vh**：html/body/#app/.screen/#gameScreen 全部改用视口单位，不依赖父元素高度
3. **@supports 100dvh 加 !important**：确保移动端覆盖生效
- Commit: d1e0841

## 全面技能检查与Bug修复

### 修复内容
1. **主公技实现**：护驾(曹操)、激将(刘备)、黄天(张角)、制霸(孙策)
2. **觉醒技实现**：志继(姜维)、凿险(邓艾)、若愚(刘禅)
3. **缺失技能实现**：屯田(邓艾)
4. **魂姿觉醒条件修复**：hp<=2 → hp=1，添加英姿摸牌效果
5. **手牌拖动排序**：实现完整的拖动排序功能（mousedown/mousemove/mouseup），支持调整手牌顺序
6. **config.js AI名字池**：孝直→法正
7. **观星bug修复**（之前session）：拖动坐标clientX、取放一致性

- Commit: 277ac02, 已部署到 https://cjh-98.github.io/sanguosha/
