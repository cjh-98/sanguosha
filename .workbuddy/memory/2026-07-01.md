# 2026-07-01 工作日志

## 修复"都点不动"的严重bug

### 问题现象
用户报告"都点不动"，包括"开始游戏"按钮和"测试按钮"都无法点击。

### 根本原因
在之前的编辑中，意外删除了 `openGuicai()` 和 `openGuidao()` 函数中的 `ov.innerHTML = \`` ，导致HTML代码被当作JavaScript代码执行，造成语法错误：
```
SyntaxError: Unexpected token '<'
```

这个语法错误导致整个inline script无法执行，所有全局函数（如 `goSetup()`）都没有被定义，所以所有按钮都点不动。

### 修复内容
1. **`openGuicai()` 函数**（第2076行）：添加了 `ov.innerHTML=\`` 
2. **`openGuidao()` 函数**（第2126行）：添加了 `ov.innerHTML=\``
3. **清理测试代码**：移除了"测试按钮"和 `initStatus` div

### 验证
- Inline script语法检查通过
- 游戏已重新部署，等待用户测试

## 多轮代码扫描和修复（10轮）

### 第1轮：修复洛神弹窗重复弹出
- 给洛神确认弹窗添加唯一id `luoshenConfirmOverlay`
- `openLuoshenUI()` 增加防止重复弹出检查
- `cancelLuoshen()` 正确移除所有洛神相关弹窗
- `startLuoshen()` 关闭确认弹窗
- `finishLuoshen()` 增加防止重复调用保护

### 第2轮：检查doJudge和判定流程
- 判定流程正确，鬼才/鬼道的人类玩家交互逻辑正确

### 第3轮：检查所有英雄技能定义
- heroes.js 中的技能定义正确

### 第4轮：检查卡牌使用和技能执行逻辑
- useCard、useSkill 函数逻辑正确

### 第5轮：检查AI逻辑
- ai.js 中的AI决策逻辑基本正确
- 修复了 `aiPlayLoop()` 的异步处理问题

### 第6轮：检查UI交互和事件处理
- updateBoard 函数正确
- UI事件处理正确

### 第7轮：检查游戏初始化和配置
- config.js 配置正确
- 游戏初始化流程正确

### 第8轮：检查伤害计算和胜负判定
- dealDamage、checkGameOver 函数逻辑正确

### 第9轮：检查DOM操作和事件监听
- DOM操作正确，使用onclick内联属性
- 事件监听正确

### 第10轮：检查游戏主循环和状态管理
- 游戏状态机正确
- 事件系统正确

## 修复的bug汇总

1. **洛神日志误导** - 日志说"你得到♥/♦"但实际没有获得，已修复为"洛神判定结果为红色，洛神结束"
2. **洛神弹窗重复** - 已添加id检查和防止重复弹出逻辑
3. **异步流程问题** - `aiPlayLoop()`、`doDiscard()`、`nextTurn()` 已修复为正确使用async/await
4. **`finishLuoshen()` 中的setTimeout** - 移除不必要的setTimeout，直接调用 `advancePhase()`

## 待修复的问题
- 无

## 用户反馈的问题

### 问题1：洛神判定到红色牌时没有获得牌
- **原因**：这是正确行为。洛神技能规则：判定到黑色牌才获得，红色牌结束技能且不获得牌。
- **修复**：日志不再误导用户，改为"洛神判定结果为红色，洛神结束"

### 问题2：洛神弹窗重复弹出
- **原因**：`openLuoshenUI()` 没有检查是否已经弹出了确认框
- **修复**：添加id检查，防止重复弹出

### 问题3：所有按钮点不动
- **原因**：inline script语法错误
- **修复**：添加缺失的 `ov.innerHTML = \``
## 出牌动画优化

### 需求
- 出牌速度再慢一点
- 牌在桌面上停留时间更长
- 只有连续操作时才让前一张牌快速消失

### 实现
1. **延长动画停留时间**：从默认的1.5秒延长到2.5秒
2. **连续出牌处理**：当连续出牌时，前一张同类型牌快速淡出（0.3秒）
3. **动画效果优化**：添加缩放、旋转、阴影等效果
4. **位置定位**：
   - 自己出牌：显示在手牌区域上方
   - 其他玩家出牌：显示在对应玩家位置

### 修改的文件
- `js/ui/board.js`：添加 `showCardAnimation()` 函数
- `css/style.css`：添加 `.card-animation` 样式和 `@keyframes cardFloat` 动画

### 部署
- 已推送到 GitHub
- 线上地址：https://cjh-98.github.io/sanguosha/
## 修复玩家选择卡牌问题 + 完善日志系统

### 修复的问题
1. **顺手牵羊/过河拆桥没有让用户选择牌**
   - 原因：`chooseCard()` 函数在人类玩家时返回 Promise，但 UI 没有正确处理
   - 修复：在 `board.js` 的 `updateGameBoard` 中添加 `_pendingCardChoice` 检查
   - 添加 `showCardSelector()` 函数让用户选择具体哪张牌

2. **火攻没有让用户选择弃哪张牌**
   - 原因：直接选择第一张同花色牌
   - 修复：改为调用 `chooseCard()` 让用户选择

### 日志系统优化
1. **log() 自动记录到 matchLog**
   - 所有 `this.log()` 调用都会自动添加到 `matchLog`

2. **实时日志上传**
   - 每30秒自动上传一次日志片段到 sanguosha-data 仓库
   - 使用 `auto-submit` 和 `debug-log` 标签

3. **对局结束保存完整日志**
   - 保存到 localStorage
   - 可以在 submit-log.html 页面提交到 GitHub

### 修改的文件
- `js/core/game.js`：修复 chooseCard、添加实时日志上传
- `js/ui/board.js`：添加 showCardSelector 函数
## 添加实时Bug日志提交系统

### 功能
1. **游戏界面添加"🐛 提交Bug"按钮**
   - 位于底部操作栏，点击弹出Bug报告窗口

2. **Bug报告窗口**
   - Bug描述输入框（可选）
   - 显示当前游戏状态摘要
   - 点击"提交日志"上传到GitHub

3. **全局函数 sgsSubmitBugLog()**
   - 游戏卡死时按 F12 打开控制台
   - 输入 `sgsSubmitBugLog()` 即可提交
   - 自动从 localStorage 读取对局日志

4. **日志内容**
   - 用户ID、对局ID
   - 当前回合、阶段、玩家
   - 牌堆/弃牌堆数量
   - 所有玩家状态（体力、手牌数等）
   - 最近100条操作日志

5. **Token处理**
   - 使用硬编码token（类似texas-poker）
   - 无需用户每次输入token

### 提交的Issue标签
- `bug-report` - Bug报告
- `auto-submit` - 自动提交
- `debug-log` - 调试日志（控制台提交时）

### 修改的文件
- `index.html`：添加按钮、样式、提交逻辑
## 完善Bug提交按钮和日志收集

### 修改内容

#### 1. 在所有页面添加提交Bug按钮
- 主菜单（底部）
- 设置页面（右上角header）
- 选将页面（右上角header）
- 身份展示页面（底部）
- 游戏中（底部操作栏，已有）
- 游戏结束页面（底部）
- 日志面板（右上角header）

#### 2. 修复Bug描述输入框文字颜色
- 从白色改为黑色，便于在暗色背景上查看

#### 3. 完善日志收集
- 收集当前页面信息
- 收集对局历史记录（localStorage）
- 包含完整的游戏状态和日志

#### 4. 提交的Issue包含
- 用户信息（ID、对局ID）
- 当前页面
- Bug描述（用户输入）
- 游戏状态JSON（回合、阶段、玩家等）
- 对局日志（最近100条）
- 对局历史（最近对局记录）

### 修改的文件
- `index.html`：添加按钮、完善日志收集逻辑
## 修复手机横屏问题

### 问题
手机打开网页一直提示"请横屏"，用户必须把手机横过来才能玩

### 修复方案
1. **禁用竖屏提示**：移除CSS媒体查询，手机上不再显示"请横屏使用"提示
2. **手机设备自动横屏布局**：
   - 检测是否为手机设备（UserAgent + 屏幕宽度 + 触摸设备）
   - 手机上强制使用横屏比例（2:1），让用户竖着手机玩横屏游戏
   - 桌面端保持全屏自适应

### 效果
- 手机用户竖屏即可游玩，游戏画面自动以横屏比例显示
- 不再弹出"请横屏使用"的提示
- 游戏体验更友好

### 修改的文件
- `index.html`：修改`enforceGameAspect()`函数和CSS样式

## 修复按钮点击问题和优化Bug描述

### 问题
1. 创建对局页面按钮点不动
2. 提交日志按钮点不动
3. Bug描述输入框文字颜色在深色背景上看不清

### 修复内容

#### 1. 为.btn-submit-log添加z-index
- 添加 `position: relative; z-index: 10;` 确保按钮不被其他元素覆盖

#### 2. 在showBugReportModal中确保隐藏cardSelOverlay
- 在弹窗显示前，先隐藏卡牌选择overlay
- 避免overlay覆盖在弹窗上导致无法点击

#### 3. 修复textarea文字颜色
- 从 `color:#000`（黑色）改为 `color:#f0d890`（金色）
- 添加 `box-shadow:inset 0 0 8px rgba(230,200,77,0.1)` 提高可读性
- 在深色背景 `#1a1a2e` 上金色文字更清晰

#### 4. 更新placeholder为更详细的Bug描述示例
- 从"例如：顺手牵羊没有让我选择牌，直接给了第一张..."
- 改为"请详细描述Bug，例如：在【选将页面】点击【确认】按钮时，出现【选将失败】的错误提示..."

### 修改的文件
- `index.html`：CSS样式、showBugReportModal函数、textarea样式和placeholder

## 修复选将页面selDesc元素不存在错误

### 问题
用户报告选将失败：
```
TypeError: Cannot set properties of null (setting 'textContent')
    at goHeroSelect (index.html:1364:56)
```

### 原因
代码尝试设置 `document.getElementById('selDesc').textContent`，但 `selDesc` 元素在选将页面的HTML中不存在。

### 修复
添加容错处理，检查元素是否存在再设置：
```javascript
const selDesc = document.getElementById('selDesc');
if (selDesc) {
    selDesc.textContent = ...;
}
```

### 修改的文件
- `index.html`：goHeroSelect函数

## 修复游戏界面布局和按钮遮挡问题

### 问题
1. 游戏对局中找不到提交Bug按钮
2. 对手角色太靠上，遮挡了顶部按钮栏

### 修复内容

1. **为topbar添加z-index:100**
   - 确保顶部信息栏（返回、回合信息、牌堆信息）在对手角色之上

2. **将playersTop的top从0改为34px**
   - 对手角色区域从顶部34px开始，避免遮挡顶部按钮栏
   - 34px是topbar的高度

3. **为底部操作栏按钮添加z-index:100**
   - log-toggle-btn（牌局记录按钮）
   - btn-submit-log（提交Bug按钮）
   - 确保按钮在对手角色之上，可以正常点击

### 修改的文件
- `index.html`：CSS样式（topbar、playersTop、log-toggle-btn、btn-submit-log）

## 修复手机横屏和电脑屏幕适配问题

### 手机版
- 整个游戏区域旋转90度显示，实现真正的横屏效果
- 游戏画面填满整个手机屏幕，不再有下方空白

### 电脑版
- 游戏区域填满整个可用空间，不留空白
- 根据屏幕高度自适应调整（calc(100vh - 100px)）

### 技术实现
- 手机端使用CSS transform: rotate(90deg) 实现画面旋转
- 电脑端使用calc()计算高度，自适应填满屏幕

### 修改的文件
- `index.html`：CSS样式和enforceGameAspect函数

## 简化屏幕适配逻辑

### 问题
之前的适配逻辑过于复杂，导致电脑和手机都无法正常显示

### 修复

**电脑版：**
- CSS设置 `height: 100%`
- JS设置 `height: '100%'` 填满整个可用空间

**手机版：**
- 尺寸设为 `屏幕高度 x 屏幕宽度`（交换宽高）
- 旋转90度显示
- 实现真正的横屏效果

### 修改的文件
- `index.html`：CSS样式和enforceGameAspect函数

## 修改对手玩家布局为凸型，修复电脑版屏幕适配

### 需求
1. 对手玩家布局从凹型改为凸型
2. 电脑版填满整个屏幕，不留空白
3. 不遮挡牌桌中间的文字

### 修改内容

#### 1. 对手玩家凸型布局
- 新增 `players-top-area`：牌桌上方显示前一半对手
- 新增 `players-bottom-area`：牌桌下方显示后一半对手
- 中间区域保持牌桌和日志
- 根据玩家数量自动分配：前一半在上方，后一半在下方

#### 2. 电脑版屏幕适配
- 计算app容器高度和header高度
- 游戏区域填满整个可用空间：`height = appRect.height - headerHeight`
- 不再有下方空白区域

#### 3. 修改的文件
- `index.html`：CSS样式（players-top-area、players-bottom-area）和enforceGameAspect函数
- `js/ui/board.js`：HTML结构和updateGameBoard函数中的玩家列表渲染逻辑

## 修复关键bug：添加缺失的UI脚本加载

### 问题
1. 点击选将没反应
2. 打开日志看不到日志
3. 提交日志没反应

### 根本原因
`index.html` 中缺少 `js/ui/board.js` 和 `js/ui/menu.js` 的加载
导致 `SGS.UI.Board` 未定义，所有相关功能都失效

### 修复
在 `index.html` 中添加 `board.js` 和 `menu.js` 的 script 标签

### 修改的文件
- `index.html`：添加 script 标签
## 实现弧形凸型布局

### 需求
用户希望对手玩家像截图中的三国杀游戏一样排布：
- 所有对手在上方一行
- 中间高，两边低（弧形/凸型）
- 不要遮挡顶部按钮

### 修改内容

#### 1. 弧形布局算法
- 使用二次曲线衰减计算每个玩家的垂直偏移量
- 中间玩家偏移最大（最高），两边逐渐降低
- 两边玩家略微缩小（最多12%），增强纵深感

#### 2. DOM结构简化
- 移除 `playersTopArea` 和 `playersBottomArea`
- 改用单一 `playersArcArea` 容器
- 所有对手都在上方一排显示

#### 3. CSS调整
- `.players-arc-area`：使用 `align-items: flex-end` 让底部对齐
- 通过 inline style 的 `margin-top` 控制高度差
- 修复 `player-mini.current` 的 transform 冲突

### 修改的文件
- `js/ui/board.js`：renderGameScreen和updateGameBoard函数
- `css/game.css`：player-mini.current样式
- `index.html`：CSS样式

## 全面修复英雄技能交互

### 问题
所有英雄技能都是AI自动选择，用户无法自己操作

### 修复内容

#### 1. 技能交互改为用户选择
- **制衡**：弹窗让用户选择要弃的牌（1-5张）
- **苦肉**：添加确认弹窗，用户确认后才发动
- **反间**：先选目标，再选要弃的牌
- **青囊**：先选目标，再选要弃的牌
- **仁德**：先选目标，再选要给的牌（1-2张）
- **结姻**：先选目标，再选要弃的2张牌
- **离间**：先选2个男性目标，再选要弃的牌
- **强袭**：有武器直接选目标，没武器先选牌再选目标
- **据守**：添加确认弹窗
- **挑衅**：选目标后直接发动
- **天义**：先选目标，再选要弃的2张牌
- **乱击**：选2张同花色牌发动万箭齐发

#### 2. 通用弃牌UI
- 新增 `promptDiscardForSkill()` 函数
- 支持选择1-5张牌
- 实时更新确认按钮状态
- 限制最少和最多选择数量

#### 3. 布局优化
- 弧形凸型布局：中间高两边低
- 添加max-height限制（30vh），避免挤压下方区域
- 添加过渡动画，让布局更平滑

### 修改的文件
- `js/ui/board.js`：clickSkill函数、新增promptDiscardForSkill函数
- `index.html`：CSS样式（players-arc-area）

## 修复游戏布局 - 解决弧形效果不显示

### 根本原因
排查发现三个导致布局完全失效的问题：
1. **CSS类名不匹配**：renderGameScreen使用 `game-topbar` 类名，但index.html中顶栏样式定义在 `.topbar` 上，导致游戏顶栏完全没有正确样式
2. **背景被覆盖**：game.css 中 `#gameScreen` 覆盖了 index.html 的桌面背景图
3. **弧形偏移太小**：弧线偏移使用 vh 单位，最多4vh，肉眼根本看不到效果

### 修复
1. renderGameScreen 中 `game-topbar` → `topbar`
2. 清理 game.css 中冲突的 #gameScreen 和 .game-topbar 样式
3. 弧形偏移从 vh(0-4) 改为 px(0-60)
4. margin-top → margin-bottom，配合 align-items: flex-end
5. players-arc-area min-height: 110px
6. flex-wrap: nowrap 确保一行排列
7. 缩放差异 12% → 15%

### 修改的文件
- `js/ui/board.js`：renderGameScreen HTML、getArcOffset/getArcScale/inline style
- `css/game.css`：移除冲突样式
- `index.html`：players-arc-area CSS

## 修复根因：内联旧版 updateBoard 仍被调用

### 根本原因（真正的根因）
排查发现 index.html 中仍然有一个内联的旧版 `_updateBoardImpl` 函数和
`BridgeAdapter` 适配器在使用。它会调用旧的 `updateBoard` 并渲染旧的
扁平化对手布局（横排在屏幕顶部覆盖 topbar）。

新的 `SGS.UI.Board.updateGameBoard` 渲染函数从未被调用，因为 OLD 引擎
使用 `BridgeAdapter`，不会触发 NEW 适配器的回调。

### 修复
1. 在内联 `_updateBoardImpl` 中直接添加弧形对手布局渲染
2. 移除对老 ID (playersTop, myAvatar, midBadge, equipZone) 的引用
3. 这些 ID 已经不存在了，会导致 JS 错误
4. 内联 gameScreen HTML 改为使用新的弧形结构

### 修改的文件
- `index.html`：内联 _updateBoardImpl 改为使用弧形布局

## 恢复对手立绘显示

### 问题
上次修改后，对手玩家显示变成了纯文字，没有立绘图片，看起来很丑

### 原因
内联 _updateBoardImpl 中我用了简化的 .player-mini 渲染
而不是完整的 .hero-card 渲染

### 修复
1. 改用 .hero-card 完整渲染
2. 包含立绘图片
3. 包含装备列表
4. 包含判定牌标记
5. 包含阵亡标记
6. 调整 .players-arc-area min-height: 160px
7. 添加 .hero-card 的 transform-origin: bottom center

### 修改的文件
- `index.html`：内联 _updateBoardImpl 改用 .hero-card 渲染

## 调整弧形布局 - 中间在顶，两边降低

### 问题
- 卡牌太小
- 弧形区域在屏幕中间而不是顶部
- 手牌区看不到
- 弧度太大

### 修复
1. #gameScreen 改为 justify-content: flex-start
   之前是 space-between 导致弧形区被推到中间
2. 弧形对齐改用 align-items: flex-start
   之前是 flex-end 加上 margin-bottom，导致卡牌在弧形区底部
3. 弧形偏移改用 margin-top
   中间 0，两边逐渐增加（25-40px）
4. 移除 scale 缩放效果，卡牌大小一致
5. min-height: 110px（之前160px太大）
6. 保持 .hero-card 原始大小 72x100

### 修改的文件
- `index.html`：CSS + 内联 _updateBoardImpl 弧形逻辑

## 修复宽度填充和手牌区不显示

### 问题
- 卡牌没填满整个屏幕宽度，左右有空隙
- 看不到自己的手牌和玩家信息

### 原因
1. _updateBoardImpl 中还有对已删除 ID (mhn, mhp) 的访问
   这些引用抛出错误，导致后续 renderHand 等函数没被执行
2. .players-arc-area 用 justify-content: center，卡牌只占中间

### 修复
1. 删除 mhn, mhp 的访问代码
2. 保留 hs=human 给 renderHand 用
3. .players-arc-area 改用 justify-content: space-around
   让卡牌均匀分布到全宽

## 恢复自己角色信息显示

### 问题
自己的角色信息（头像、名字、体力、身份）不显示

### 原因
_UpdateBoardImpl 引用旧 ID (mhn, mhp, myAvatar, midBadge, equipZone)
但新 HTML 用新 ID (myHeroName, myHp, myIdentity)

### 修复
1. 在 .hand-area 中恢复 .myinfo / .myinfo-left 旧结构
2. 恢复 myAvatar, mhn, mhp, midBadge, equipZone 等旧 ID
3. 旧 _updateBoardImpl 渲染代码不需要改

### 修改的文件
- `index.html`：gameScreen HTML + _updateBoardImpl
